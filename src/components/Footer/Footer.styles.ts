import { css } from '@emotion/react';
import { COLORS } from '../../styles/Colors';

export const FooterStyle = css`
    width: 100%;
    height: 150px;
    padding: 3rem 10% 3rem 10%;
    background: ${COLORS.BLACK_200};    //color에 gray생성 후 사용.
`;